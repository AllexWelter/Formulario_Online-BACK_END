// Componente apenas para testes e rascunhos do projeto.

// Endpoints:

// /users (GET): Lista todos os usuários (opcional, pode ser removido se não for necessário).

// /iniciar (POST): Inicia um novo quiz e cria um usuário, se necessário. --> OK

// /perguntas/:numero (GET): Retorna uma pergunta e suas alternativas, além do total de perguntas.

// /enviar (POST): Recebe as respostas do quiz, valida, salva no banco de dados e calcula a pontuação.

// /email/:idQuiz (GET): Envia um email com o resultado do quiz.

